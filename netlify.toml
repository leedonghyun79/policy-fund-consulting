[build]
  command = "prisma generate && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# 예시: 리다이렉트 규칙 (필요에 따라 수정하세요)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
